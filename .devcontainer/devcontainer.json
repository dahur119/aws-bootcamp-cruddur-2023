{
    "name": "Cruddur Configuration",
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "features": {
        "ghcr.io/devcontainers/features/aws-cli:1": {}
    },
    "remoteEnv": {
        "AWS_CLI_AUTO_PROMPT": "on-partial"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-azuretools.vscode-docker",
                "ms-python.python",
                "vscodevim.vim",
                "ms-ossdata.vscode-postgresql"
            ],
            "settings": {
                "terminal.integrated.fontSize": 20,
                "editor.fontSize": 20,
                "workbench.colorTheme": "Default Dark+ Experimental"
            }
        }
    },
    "postCreateCommand": "curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/postgresql.gpg && echo \"deb http://apt.postgresql.org/pub/repos/apt/ $(lsb_release -cs)-pgdg main\" | sudo tee /etc/apt/sources.list.d/pgdg.list && sudo apt update && sudo apt install -y postgresql-client-13 libpq-dev && curl \"https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip\" -o \"awscliv2.zip\" && unzip awscliv2.zip && sudo ./aws/install && rm -f awscliv2.zip && cd backend-flask && pip install -r requirements.txt && cd ../frontend-react-js && npm install",
    "forwardPorts": [
        3000, // Frontend port
        4567, // Backend port
        2000  // X-Ray daemon port (not publicly visible in this setup)
    ],
    "remoteUser": "vscode",
    "postStartCommand": "aws xray daemon start --port 2000"
}
